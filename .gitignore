node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.idea

# Python backend
backend/.venv/
backend/.env
backend/.langgraph_api/
__pycache__/
*.pyc
.ruff_cache/

# Assessment data — track queries and schema, ignore large cassettes and outputs
backend/assessment_data/cassettes/
backend/assessment_data/outputs/
backend/assessment_data/canonical/
backend/assessment_data/capture_summary.json
# Scratch spikes / build artifacts (not tracked)
tmp/

# Large vendored review PDF (~10MB) — kept local; small reference PDFs are tracked
docs/references/biomedical-link-prediction-methods-and-evidence.pdf

# code-on-graph spike: ignore transient per-item checkpoints (final result JSONs are tracked)
backend/tests/code_on_graph_spike/runs/*.partial.json

# Biomapper eval run artifacts (timestamped; persisted by default)
backend/src/kestrel_backend/evals/**/runs/
